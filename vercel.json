{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    {
      "src": "/(?!.*\\.(js|css|png|jpg|svg|ico|json))",
      "dest": "/index.html"
    }
  ]
}
